import * as camera from './cameratools';
import * as common from './commontools';
import * as input from './inputtools';
import * as network from './networktools';
import * as object from './objecttools';
import * as runtimeScene from './runtimescenetools';
import * as sound from './soundtools';
import * as storage from './storagetools';
import * as string from './stringtools';
import * as window from './windowtools';

/**
 * A namespace with multiple functions used by events code.
 * @namespace
 */
export const evtTools = {
  camera,
  common,
  input,
  network,
  object,
  runtimeScene,
  sound,
  storage,
  string,
  window,
};
