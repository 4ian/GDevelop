#include <iostream>
#include <string.h>
#include <string>
#include <vector>

using namespace std;

void rc4(unsigned char * ByteInput, unsigned char * pwd,
		   unsigned char * &ByteOutput);
