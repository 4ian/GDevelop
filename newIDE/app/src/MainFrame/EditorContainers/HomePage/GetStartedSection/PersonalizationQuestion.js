// @flow

import * as React from 'react';

type Props = {|
  step: string,
  onSelectAnswers: (string[]) => void,
|};

const PersonalizationQuestion = (props: Props) => {
  return <div>Bonjour</div>;
};

export default PersonalizationQuestion;
